# ============================================================
#  .env.sample — Copy to .env and fill in your values.
#  Never commit .env to version control.
# ============================================================

# Backend API base URL
REACT_APP_BACKEND_BASE_URL=https://your-api-base-url.com

# Theme / Brand colors (hex without #)
PRIMARY_COLOR=0f3dac
SECONDARY_COLOR=1E40AF

# App name (optional)
# REACT_APP_NAME=Admin Panel
